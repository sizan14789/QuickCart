import UploadForm from "@/app/dashboard/components/UploadForm";

const Dashboard = async () => {
  
  return (
    <div className="px-5 md:px-10 flex flex-col gap-6">
      <UploadForm />
    </div>
  );
};

export default Dashboard;
